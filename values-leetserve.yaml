# Default values for python.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
nameOverride: level
image:
  pullPolicy: Always
  repository: docker.io/dukeman/riverlevel:latest
ingress:
  enabled: true
basedomain: leetserve.com
